# ORIENT Phase 1 — optional, keine Cloud-Pflicht
# Kopieren nach .env.local für lokale Overrides

# KI-Verarbeitung (Standard: aus)
VITE_AI_ENABLED=false

# Nur wenn VITE_AI_ENABLED=true und Backend existiert (ab v2):
# VITE_AI_API_URL=
# VITE_AI_API_KEY=
