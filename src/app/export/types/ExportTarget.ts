/**
 * ORIENT - Export Target Types
 * 
 * Ziele für Exporte.
 * Respektiert ORIENT_DNA: Klarheit, keine Überkomplexität.
 */

export enum ExportTarget {
  MARKDOWN = 'MARKDOWN',
  CURSOR_PACK = 'CURSOR_PACK',
}
