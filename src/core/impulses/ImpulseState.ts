/**
 * ORIENT - Impulse State
 * 
 * Impulse-State respektiert ORIENT_DNA:
 * - Rohe Gedanken werden nicht bewertet
 * - Nicht zu früh strukturieren
 * - DUST = roh, unfertig
 * - CLOUD = leichte Verdichtung
 */

export enum ImpulseState {
  DUST = 'DUST',     // roh, unfertig
  CLOUD = 'CLOUD',   // leichte Verdichtung
}
